<% if signed_in? %>


	<% if current_dstdoctor %>
	      의사로 로그인 / 
	      <%= current_dstdoctor.email %> / 
	      <%= link_to "Sign out", destroy_dstdoctor_session_path, method: :delete %>

	<% elsif current_dpatient %>
	      환자로 로그인 / 
	      <%= current_dpatient.email %> / 
	      <%= link_to "Sign out", destroy_dpatient_session_path, method: :delete %>

	<% end %>


<% else %>
	stdoctor / 
	<%= link_to "Sign in", new_dstdoctor_session_path %> / 
	<%= link_to "Sign up", new_dstdoctor_registration_path %> <br>

	patient / 
	<%= link_to "Sign in", new_dpatient_session_path %> / 
	<%= link_to "Sign up", new_dpatient_registration_path %> <br>	

<% end %>
<br>------------------------------------------------------------ <br>